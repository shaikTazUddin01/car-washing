import { Types } from "mongoose";
import { Auth } from "../Auth/auth.model";
import { Service, Slot } from "../service/service.model";

export const checkExists=async(customerId :Types.ObjectId,service:string,slot:string)=>{

   
}